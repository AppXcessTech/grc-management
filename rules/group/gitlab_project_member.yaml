source_table: gitlab_project_member
canonical_category: Group
provider: GitLab
canonical_id_template: $.id or $.user_id or $.username

mappings:
  display_name: $.username or $.name or $.id
  access_level: $.access_level
  state: $.state
  expires_at: $.expires_at
  created_at: $.created_at

required_canonical_fields:
  - display_name

defaults:
  source_system: gitlab

version: 1
last_modified: '2026-07-30'
