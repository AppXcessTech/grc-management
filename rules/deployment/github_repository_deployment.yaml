source_table: github_repository_deployment
canonical_category: Repository
provider: GitHub
token_type: fine_grained
canonical_id_template: $.node_id or $.id or $.environment_name or $.repository_full_name

mappings:
  display_name: $.environment or $.description or $.task or $.node_id or $.repository_full_name

required_canonical_fields:
  - display_name

defaults:
  source_system: github

version: 2
last_modified: '2026-07-28'
