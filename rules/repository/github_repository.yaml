source_table: github_repository
canonical_category: Repository
provider: GitHub
token_type: fine_grained
canonical_id_template: $.node_id or $.id or $.full_name or $.name

mappings:
  display_name: $.full_name or $.name or $.node_id

required_canonical_fields:
  - display_name

defaults:
  source_system: github

version: 1
last_modified: '2026-07-28'
